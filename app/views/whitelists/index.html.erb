<section class="container">

<div class="panel panel-default">
  <div class="panel-heading">
    <h3>Whitelist Beta</h3>
  </div>
  <div class="panel-body">
    <div class="row">
      <div class="col-sm-6">
        <%= render 'new' %>
      </div>
      <div class="col-sm-6">
        <%= search_form_for @search do |f|%>
        <div class="field">
          <%= f.text_field :email_cont, placeholder: "Email contient" %>
          <%= f.submit "Chercher" %>
        </div>
        <% end %>
      </div>
    </div>
    <div id="whitelist">
      <%= render 'index' %>
    </div>
  </div>
  <div class="panel-footer">
    <div class="row">
      <div class="col-sm-3 text-center" style="margin-top:10px;">
        <%= link_to "Retour", admin_panel_path, class:"btn btn-default" %>
      </div>
      <div class="col-sm-6 text-center">
        <%= will_paginate @whitelists, :previous_label => "Précédent", :next_label => "Suivant" %>
      </div>
      <div class="col-sm-3"></div>
    </div>
  </div>
</div>

</section>
