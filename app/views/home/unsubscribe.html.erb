<div class="container">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3>Notifications Email</h3>
    </div>
    <div class="panel-body" style="min-height:200px;padding:100px 20px;">
      <% if params[:email].present? && !@user.nil? %>
        <div class="alert alert-success text-center">Vous avez été correctement désinscrit des notifications par mail.</div>
      <% else %>
        <div class="alert alert-danger text-center">Email non reconnu, merci de contacter un administrateur en utilisant la fenêtre de discussion en bas à droite de votre écran.</div>
      <% end %>
    </div>
  </div>
</div>
