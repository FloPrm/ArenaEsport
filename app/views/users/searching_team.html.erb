<div class="row">

    <div class="col-lg-3 hidden-sm hidden-xs">
    </div>

    <div class="col-lg-6 col-sm-12 col-xs-12" style="margin:auto;">
      <div class="panel panel-default">
        <div class="panel-heading text-center">
          <h3>Recherche en cours</h3>
        </div>
        <div class="panel-body">
          <%= render 'users/search'%>
        </div>
        <div class="panel-footer container-fluid">
          <%= render 'users/mon_equipe_btn' %>
        </div>
      </div>
      <% if current_user.state == 1 && current_user.active_subscription.nil? %>
        <p class="text-center">Tu n'as pas envie d'attendre ? Passe premium et devient prioritaire ! <%= link_to "En savoir plus", pricing_path %></p>
      <% end %>
    </div>

    <div class="col-lg-3 hidden-sm hidden-xs">
    </div>


</div>
